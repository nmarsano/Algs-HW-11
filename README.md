# Algs-HW-11
block stacking dynamic programming algorithm

TO RUN: in terminal type "python blocks.py 'input.txt' 'output.txt'"

FILES:
block.py contains all parts of this algorithm including
 -  a class Blocks that holds the dimensions of block and string interpretation of the block
 -  a function to recieve the area of a block's base dimensions to sort the array of all possible blocks used in stack
